name: Deploy Frontend Main to Vercel Staging

on:
  push:
    branches:
      - main
    paths:
      - 'frontends/frontend-main/**'
      - 'package.json'
      - 'yarn.lock'
  workflow_dispatch:

env:
  VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
  VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_FRONTEND_MAIN }}

jobs:
  deploy:
    name: Deploy Frontend Main
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'yarn'

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Pull Vercel Environment
        run: vercel pull --yes --environment=production --token=${{ secrets.VERCEL_TOKEN }}
        working-directory: frontends/frontend-main

      - name: Install Dependencies
        run: yarn install --frozen-lockfile

      - name: Build Project
        run: vercel build --prod --token=${{ secrets.VERCEL_TOKEN }}
        working-directory: frontends/frontend-main

      - name: Deploy to Vercel
        id: deploy
        run: |
          url=$(vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }})
          echo "url=$url" >> $GITHUB_OUTPUT
        working-directory: frontends/frontend-main

      - name: Deployment Summary
        run: |
          echo "✅ Frontend Main deployed to: ${{ steps.deploy.outputs.url }}"
